export default function TimedQuiz (props)
{
    let totalQuizTime;
    let timeBetweenHints;
    let scoreMultiplier;

    return(
        <div className = "pokeQuiz">
         
            
        </div>
    );
 
}